// Code generated by SQLBoiler 4.14.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// BuffaloProduct is an object representing the database table.
type BuffaloProduct struct {
	Name        null.String `boil:"name" json:"name,omitempty" toml:"name" yaml:"name,omitempty"`
	Jan         null.String `boil:"jan" json:"jan,omitempty" toml:"jan" yaml:"jan,omitempty"`
	Price       null.Int64  `boil:"price" json:"price,omitempty" toml:"price" yaml:"price,omitempty"`
	ShopCode    string      `boil:"shop_code" json:"shop_code" toml:"shop_code" yaml:"shop_code"`
	ProductCode string      `boil:"product_code" json:"product_code" toml:"product_code" yaml:"product_code"`
	URL         null.String `boil:"url" json:"url,omitempty" toml:"url" yaml:"url,omitempty"`

	R *buffaloProductR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L buffaloProductL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var BuffaloProductColumns = struct {
	Name        string
	Jan         string
	Price       string
	ShopCode    string
	ProductCode string
	URL         string
}{
	Name:        "name",
	Jan:         "jan",
	Price:       "price",
	ShopCode:    "shop_code",
	ProductCode: "product_code",
	URL:         "url",
}

var BuffaloProductTableColumns = struct {
	Name        string
	Jan         string
	Price       string
	ShopCode    string
	ProductCode string
	URL         string
}{
	Name:        "buffalo_products.name",
	Jan:         "buffalo_products.jan",
	Price:       "buffalo_products.price",
	ShopCode:    "buffalo_products.shop_code",
	ProductCode: "buffalo_products.product_code",
	URL:         "buffalo_products.url",
}

// Generated where

var BuffaloProductWhere = struct {
	Name        whereHelpernull_String
	Jan         whereHelpernull_String
	Price       whereHelpernull_Int64
	ShopCode    whereHelperstring
	ProductCode whereHelperstring
	URL         whereHelpernull_String
}{
	Name:        whereHelpernull_String{field: "\"buffalo_products\".\"name\""},
	Jan:         whereHelpernull_String{field: "\"buffalo_products\".\"jan\""},
	Price:       whereHelpernull_Int64{field: "\"buffalo_products\".\"price\""},
	ShopCode:    whereHelperstring{field: "\"buffalo_products\".\"shop_code\""},
	ProductCode: whereHelperstring{field: "\"buffalo_products\".\"product_code\""},
	URL:         whereHelpernull_String{field: "\"buffalo_products\".\"url\""},
}

// BuffaloProductRels is where relationship names are stored.
var BuffaloProductRels = struct {
}{}

// buffaloProductR is where relationships are stored.
type buffaloProductR struct {
}

// NewStruct creates a new relationship struct
func (*buffaloProductR) NewStruct() *buffaloProductR {
	return &buffaloProductR{}
}

// buffaloProductL is where Load methods for each relationship are stored.
type buffaloProductL struct{}

var (
	buffaloProductAllColumns            = []string{"name", "jan", "price", "shop_code", "product_code", "url"}
	buffaloProductColumnsWithoutDefault = []string{"shop_code", "product_code"}
	buffaloProductColumnsWithDefault    = []string{"name", "jan", "price", "url"}
	buffaloProductPrimaryKeyColumns     = []string{"shop_code", "product_code"}
	buffaloProductGeneratedColumns      = []string{}
)

type (
	// BuffaloProductSlice is an alias for a slice of pointers to BuffaloProduct.
	// This should almost always be used instead of []BuffaloProduct.
	BuffaloProductSlice []*BuffaloProduct
	// BuffaloProductHook is the signature for custom BuffaloProduct hook methods
	BuffaloProductHook func(context.Context, boil.ContextExecutor, *BuffaloProduct) error

	buffaloProductQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	buffaloProductType                 = reflect.TypeOf(&BuffaloProduct{})
	buffaloProductMapping              = queries.MakeStructMapping(buffaloProductType)
	buffaloProductPrimaryKeyMapping, _ = queries.BindMapping(buffaloProductType, buffaloProductMapping, buffaloProductPrimaryKeyColumns)
	buffaloProductInsertCacheMut       sync.RWMutex
	buffaloProductInsertCache          = make(map[string]insertCache)
	buffaloProductUpdateCacheMut       sync.RWMutex
	buffaloProductUpdateCache          = make(map[string]updateCache)
	buffaloProductUpsertCacheMut       sync.RWMutex
	buffaloProductUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var buffaloProductAfterSelectHooks []BuffaloProductHook

var buffaloProductBeforeInsertHooks []BuffaloProductHook
var buffaloProductAfterInsertHooks []BuffaloProductHook

var buffaloProductBeforeUpdateHooks []BuffaloProductHook
var buffaloProductAfterUpdateHooks []BuffaloProductHook

var buffaloProductBeforeDeleteHooks []BuffaloProductHook
var buffaloProductAfterDeleteHooks []BuffaloProductHook

var buffaloProductBeforeUpsertHooks []BuffaloProductHook
var buffaloProductAfterUpsertHooks []BuffaloProductHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *BuffaloProduct) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffaloProductAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *BuffaloProduct) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffaloProductBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *BuffaloProduct) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffaloProductAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *BuffaloProduct) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffaloProductBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *BuffaloProduct) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffaloProductAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *BuffaloProduct) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffaloProductBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *BuffaloProduct) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffaloProductAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *BuffaloProduct) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffaloProductBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *BuffaloProduct) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffaloProductAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddBuffaloProductHook registers your hook function for all future operations.
func AddBuffaloProductHook(hookPoint boil.HookPoint, buffaloProductHook BuffaloProductHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		buffaloProductAfterSelectHooks = append(buffaloProductAfterSelectHooks, buffaloProductHook)
	case boil.BeforeInsertHook:
		buffaloProductBeforeInsertHooks = append(buffaloProductBeforeInsertHooks, buffaloProductHook)
	case boil.AfterInsertHook:
		buffaloProductAfterInsertHooks = append(buffaloProductAfterInsertHooks, buffaloProductHook)
	case boil.BeforeUpdateHook:
		buffaloProductBeforeUpdateHooks = append(buffaloProductBeforeUpdateHooks, buffaloProductHook)
	case boil.AfterUpdateHook:
		buffaloProductAfterUpdateHooks = append(buffaloProductAfterUpdateHooks, buffaloProductHook)
	case boil.BeforeDeleteHook:
		buffaloProductBeforeDeleteHooks = append(buffaloProductBeforeDeleteHooks, buffaloProductHook)
	case boil.AfterDeleteHook:
		buffaloProductAfterDeleteHooks = append(buffaloProductAfterDeleteHooks, buffaloProductHook)
	case boil.BeforeUpsertHook:
		buffaloProductBeforeUpsertHooks = append(buffaloProductBeforeUpsertHooks, buffaloProductHook)
	case boil.AfterUpsertHook:
		buffaloProductAfterUpsertHooks = append(buffaloProductAfterUpsertHooks, buffaloProductHook)
	}
}

// One returns a single buffaloProduct record from the query.
func (q buffaloProductQuery) One(ctx context.Context, exec boil.ContextExecutor) (*BuffaloProduct, error) {
	o := &BuffaloProduct{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for buffalo_products")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all BuffaloProduct records from the query.
func (q buffaloProductQuery) All(ctx context.Context, exec boil.ContextExecutor) (BuffaloProductSlice, error) {
	var o []*BuffaloProduct

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to BuffaloProduct slice")
	}

	if len(buffaloProductAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all BuffaloProduct records in the query.
func (q buffaloProductQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count buffalo_products rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q buffaloProductQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if buffalo_products exists")
	}

	return count > 0, nil
}

// BuffaloProducts retrieves all the records using an executor.
func BuffaloProducts(mods ...qm.QueryMod) buffaloProductQuery {
	mods = append(mods, qm.From("\"buffalo_products\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"buffalo_products\".*"})
	}

	return buffaloProductQuery{q}
}

// FindBuffaloProduct retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBuffaloProduct(ctx context.Context, exec boil.ContextExecutor, shopCode string, productCode string, selectCols ...string) (*BuffaloProduct, error) {
	buffaloProductObj := &BuffaloProduct{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"buffalo_products\" where \"shop_code\"=$1 AND \"product_code\"=$2", sel,
	)

	q := queries.Raw(query, shopCode, productCode)

	err := q.Bind(ctx, exec, buffaloProductObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from buffalo_products")
	}

	if err = buffaloProductObj.doAfterSelectHooks(ctx, exec); err != nil {
		return buffaloProductObj, err
	}

	return buffaloProductObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *BuffaloProduct) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no buffalo_products provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(buffaloProductColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	buffaloProductInsertCacheMut.RLock()
	cache, cached := buffaloProductInsertCache[key]
	buffaloProductInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			buffaloProductAllColumns,
			buffaloProductColumnsWithDefault,
			buffaloProductColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(buffaloProductType, buffaloProductMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(buffaloProductType, buffaloProductMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"buffalo_products\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"buffalo_products\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into buffalo_products")
	}

	if !cached {
		buffaloProductInsertCacheMut.Lock()
		buffaloProductInsertCache[key] = cache
		buffaloProductInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the BuffaloProduct.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *BuffaloProduct) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	buffaloProductUpdateCacheMut.RLock()
	cache, cached := buffaloProductUpdateCache[key]
	buffaloProductUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			buffaloProductAllColumns,
			buffaloProductPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update buffalo_products, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"buffalo_products\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, buffaloProductPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(buffaloProductType, buffaloProductMapping, append(wl, buffaloProductPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update buffalo_products row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for buffalo_products")
	}

	if !cached {
		buffaloProductUpdateCacheMut.Lock()
		buffaloProductUpdateCache[key] = cache
		buffaloProductUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q buffaloProductQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for buffalo_products")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for buffalo_products")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BuffaloProductSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), buffaloProductPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"buffalo_products\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, buffaloProductPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in buffaloProduct slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all buffaloProduct")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *BuffaloProduct) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no buffalo_products provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(buffaloProductColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	buffaloProductUpsertCacheMut.RLock()
	cache, cached := buffaloProductUpsertCache[key]
	buffaloProductUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			buffaloProductAllColumns,
			buffaloProductColumnsWithDefault,
			buffaloProductColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			buffaloProductAllColumns,
			buffaloProductPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert buffalo_products, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(buffaloProductPrimaryKeyColumns))
			copy(conflict, buffaloProductPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"buffalo_products\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(buffaloProductType, buffaloProductMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(buffaloProductType, buffaloProductMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert buffalo_products")
	}

	if !cached {
		buffaloProductUpsertCacheMut.Lock()
		buffaloProductUpsertCache[key] = cache
		buffaloProductUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single BuffaloProduct record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *BuffaloProduct) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no BuffaloProduct provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), buffaloProductPrimaryKeyMapping)
	sql := "DELETE FROM \"buffalo_products\" WHERE \"shop_code\"=$1 AND \"product_code\"=$2"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from buffalo_products")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for buffalo_products")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q buffaloProductQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no buffaloProductQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from buffalo_products")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for buffalo_products")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BuffaloProductSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(buffaloProductBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), buffaloProductPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"buffalo_products\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, buffaloProductPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from buffaloProduct slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for buffalo_products")
	}

	if len(buffaloProductAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *BuffaloProduct) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindBuffaloProduct(ctx, exec, o.ShopCode, o.ProductCode)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BuffaloProductSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := BuffaloProductSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), buffaloProductPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"buffalo_products\".* FROM \"buffalo_products\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, buffaloProductPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in BuffaloProductSlice")
	}

	*o = slice

	return nil
}

// BuffaloProductExists checks if the BuffaloProduct row exists.
func BuffaloProductExists(ctx context.Context, exec boil.ContextExecutor, shopCode string, productCode string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"buffalo_products\" where \"shop_code\"=$1 AND \"product_code\"=$2 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, shopCode, productCode)
	}
	row := exec.QueryRowContext(ctx, sql, shopCode, productCode)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if buffalo_products exists")
	}

	return exists, nil
}

// Exists checks if the BuffaloProduct row exists.
func (o *BuffaloProduct) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return BuffaloProductExists(ctx, exec, o.ShopCode, o.ProductCode)
}
